// ESC > 화살표 함수 

// 선언식 
function plus(a, b) {
    return a + b;

}
// 표현식
const plus = function (a, b) {
    return a + b;
};

//function 키워드를 생략하고 화살표 사용
const plus = (a, b) => a + b;

//화살표 함수의 매개변수가 하나인 경우 괄호 생략 가능
const plus1 = a => a + 1;

// 화살표 함수의 매개변수가 

